#  first we  will import it
#  we can make changes in operating  system

import os
# this code below will create a directory mean a folder
# let first make a check if that folder is Already there

# if(not os.path.exists("data")): 
#     os.mkdir("data")


# 1.let us create  large number of folders
# it takes the path and in second it take the folder names to be created
# for i in range(0, 10):
#     os.mkdir(f"data/Day{i+1}")

# 2. Let us rename the folders
# for i in range(0, 10):
#     os.rename(f"data/Day{i+1}", f"data/tut{i+1}")

# folders = os.listdir("data")

# for x in folders:

#     print(folders)  
#     print(os.listdir(f"data/{x}"))

